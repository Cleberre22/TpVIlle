{% extends 'base.html.twig' %}

{% block title %}SocialNetwork index{% endblock %}

{% block body %}
    <h1>Réseaux social</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Lien</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for social_network in social_networks %}
            <tr>
                <td>{{ social_network.id }}</td>
                <td>{{ social_network.name }}</td>
                <td>{{ social_network.link }}</td>
                <td><img src="{{ asset('uploads/photos/' ~ social_network.img) }}" width="100"></td>
                <td>
                    <a href="{{ path('app_social_network_show', {'id': social_network.id}) }}">show</a>
                    <a href="{{ path('app_social_network_edit', {'id': social_network.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_social_network_new') }}">Create new</a>
{% endblock %}
